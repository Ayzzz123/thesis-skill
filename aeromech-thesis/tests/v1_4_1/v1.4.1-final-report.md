# v1.4.1 最终报告（Stability & Production Hardening）

## 【版本】
**v1.4.1**（aeromech-thesis；1.4.0 → 1.4.1；定位：稳定性与产品化，不新增大型功能）

## 【升级前审计结论】
- 确认基线为 **v1.4.0**（SKILL.md 版本行 / CHANGELOG / 两份最终报告 / tests/v1_4 齐备）。
- 遗留问题清单（本版本逐项处置）：
  1) 开发目录 `Desktop\thesis-skill` 缺失，安装目录被当作唯一开发源；
  2) `tf_qa` 无模板项目无法运行（要求 --template；此前只能"不运行+披露"）；
  3) RQG-09/RQG-10 等 Critical 级语义检查为启发式，清洁时自动 PASS；
  4) QA/Registry/Gate 输出缺统一状态码/severity/reason/remediation；异常路径未定义行为。

## 【修复的问题】
| 编号 | 问题 | 修复 |
|---|---|---|
| F-01 | 开发/安装目录关系缺失（dev 目录不存在） | 重建 `Desktop\thesis-skill\aeromech-thesis`（权威开发源）+ `sync.py`（--check/--to-install/--to-dev，镜像+逐字节校验）+ DEV_SYNC.md / INSTALL_SYNC.md；`test_dev_install_sync` 断言两侧一致与版本一致 |
| F-02 | `tf_qa` 无模板即"不可运行/异常状态" | `--template` 可省略（或 none）：模板对照项（TF-01~04、TF-19）输出 **NOT_APPLICABLE**（附 reason），自含检查照常执行，退出码 0（Delivery Gate 不因无模板失败）；模板路径不存在 = 配置错误（退出码 3，不静默降级） |
| F-03 | 启发式语义检查自动 PASS（RQG-09/RQG-10） | 弱证据（全 simulated/pending）语境下未命中模式 → **NEEDS_HUMAN_REVIEW** + `human-review-checklist.md`（claim/evidence/reason/uncertainty）+ Gate `PASS_WITH_HUMAN_REVIEW`；`human-review.yaml` 裁决回路（ok→PASS；violation→FAIL Critical） |
| F-04 | 输出无统一状态码/severity/reason/remediation；异常可能裸崩 | 全 RI 层状态词表（PASS/WARN/FAIL/NHR/NOT_APPLICABLE/SKIPPED_WITH_REASON/ERROR）；FAIL 附 severity+reason+remediation；注册表损坏 → RI-CORRUPT(critical)/validate rc1、trace/coverage rc3；内部异常 → gate=ERROR rc3，并覆盖错误报告（防止陈旧 PASS 被误读） |
| F-05 | `tf_qa` TF-13 无页眉时 f-string 提前求值 TypeError（潜在崩溃） | 延迟构造消息（由 test_false_pass_prevention 捕获） |
| F-06 | `sync.py` filecmp 缓存（键不含 mtime）导致同尺寸文件覆盖后仍报差异 | 改逐字节比较（由 test_dev_install_sync 负例捕获） |

## 【新增机制】
1. **统一状态模型**（`references/research-integrity.md` §19 / `delivery-pipeline.md` §8.1）：7 状态词表 + 退出码语义（0/1/2/3）。
2. **NEEDS_HUMAN_REVIEW 人工复核回路**：`references/research-human-review.md`（七维复核清单）+ 自动生成 `human-review-checklist.md` + `.aeromech/research/human-review.yaml` 裁决消费。
3. **无模板 QA（NOT_APPLICABLE）**：`delivery-pipeline.md` §8.2 规程 + tf_qa 实现；N/A 不计 FAIL、不伪造 PASS、交付报告须如实披露。
4. **开发/安装目录同步规程**：`Desktop\thesis-skill`（DEV_SYNC.md / INSTALL_SYNC.md / sync.py；忽略运行期证据，策展报告照常同步）。

## 【NOT_APPLICABLE 行为（实测）】
- `tf_qa --docx <论文> --pdf <论文>`（省略 --template）于 **test-6.0**（零模板项目）：
  `PASS=15 FAIL=0 NOT_APPLICABLE=5 SKIPPED_WITH_REASON=0`，退出码 **0**；
  N/A 项 = TF-01/02/03（封面母版对照）、TF-04（声明页对照）、TF-19（页边距基准），均附 reason；
  自含检查（摘要/ABSTRACT/正文格式/图表题注/三线表/页码/目录域/参考文献/附录/致谢/TF-20 渲染复核）照常执行。
- `--template none` 等价；`--template <不存在路径>` → 退出码 3（配置错误），不静默降级。
- tests/v1_4_1/test_not_applicable_qa：19 项断言验证"N/A 绝不显示为 PASS"与模板模式不出现 N/A。

## 【NEEDS_HUMAN_REVIEW 行为（实测）】
- **test-6.0**（全模拟数据）：RQG-09、RQG-10 → `NEEDS_HUMAN_REVIEW`；队列 4 项
  `['RQG-09:ABSTRACT','RQG-10:CL-003','RQG-10:CL-006','RQG-10:CON-002']`（每项含 claim/evidence/reason/uncertainty）；
  gate=`PASS_WITH_HUMAN_REVIEW`，退出码 0；生成 `human-review-checklist.md`。
- **裁决回路实测**：4 项全部 `ok` → gate 回到 `PASS`（resolved=4）；任一 `violation` → gate `FAIL`（Critical，退出码 1）；
  未知 decision / 未匹配键 → WARN（不阻断）。演示取证后已移除裁决文件，test-6.0 保持"待人工复核"的诚实状态。
- 边界：强断言越界（"机队发生率为…"）仍 **FAIL Critical**（未被 NHR 软化）；CL-002（模拟+已验证混合证据）不误入队列。

## 【新增测试数量】
`tests/v1_4_1/`：**5 个测试文件（82 项断言，5/5 通过）**
- test_not_applicable_qa（19）｜test_human_review（22）｜test_gate_state（13）｜test_dev_install_sync（12）｜test_false_pass_prevention（16）

## 【回归结果】PASS（详见 `tests/v1_4_1/regression/regression-report.md`）
| 维度 | 结果 |
|---|---|
| tests/v1_4（11 文件） | 11/11 PASS（期望值按 v1.4.1 语义更新：全模拟夹具 → PASS_WITH_HUMAN_REVIEW） |
| test_a / test_b（模板保真/重建） | 12/0 与 6/0 PASS |
| Template Fidelity | test-3.0 PASS=20/0、test-4.0 PASS=20/0、test-5.0 PASS=18/0(+2 SKIP)、test-6.0 PASS=15/0(+5 N/A) |
| Cover Fidelity | test-4.0 25/0、test-5.0 25/0 |
| Table QA | test-4.0 22/0、test-5.0 22/0 与 table_readability 11/0 |
| Graph QA | test-4.0 0 FAIL、test-5.0 68 PASS |
| PDF QA | test-3.0/4.0/5.0/6.0 全 PASS（Critical 0/High 0） |
| Research Quality | test-6.0 机械检查不回归；语义项升级为 NHR（设计内）；test-5.0 旧项目 rc=2 不阻塞 |
| Delivery Gate | 状态机五态实测（v1_4_1 套件）；无因无模板/正常路径误 FAIL |

## 【开发/安装目录状态】
- 开发目录：`C:\Users\29603\Desktop\thesis-skill\aeromech-thesis`（**已重建**，v1.4.1 起为权威开发源）；
- 安装目录：`C:\Users\29603\.qoder-cn\skills\aeromech-thesis`（部署副本）；
- `sync.py --check`：**IDENTICAL**（逐字节比较；忽略 __pycache__/*.log/运行期 regression 证据）；
- 说明文档：`Desktop\thesis-skill\DEV_SYNC.md`、`INSTALL_SYNC.md`；同步器自测（负例）通过。

## 【版本管理】
SKILL.md（v1.4.1 + §19.1 增补）、README.md（当前版本 + 版本记录）、CHANGELOG.md（v1.4.1 章节）、
references/research-integrity.md（§19 状态模型）、references/delivery-pipeline.md（§8.1/8.2）、
references/state.md（§17 门禁联动 4/5 条）、references/research-human-review.md（新增）。

## 【最终 Delivery Gate】
**PASS**
- v1.4.1 全量测试：tests/v1_4 11/11、tests/v1_4_1 5/5、test_a/test_b 通过；
- 四个既有项目（3.0/4.0/5.0/6.0）关键 QA 无回归；test-6.0 交付物状态 = `PASS_WITH_HUMAN_REVIEW`（待人工复核项已列清单，属设计内语义）；
- Critical/High（v1.4.1 范围内）：**0**。

---

**最终结论：v1.4.1 = PASS**
