# v1.6.5 Release Report — Academic Figure Quality & User-Configured Image Provider

- 发布日期：2026-09-19
- **version = v1.6.5**（SKILL.md / README.md / CHANGELOG.md 顶部条目三处一致；历史条目 v1.6.0 及更早保持原样）
- **release_readiness = PASS**（RELEASE_READY=YES，报告见 v1.6.5-release-readiness.md；用户确认后进入 Release）
- **TEST_8_0 = PASS**（thesis_build pipeline 全阶段 PASS）
- **Delivery Gate = PASS**（orchestrator `gate=PASS · blocked=False`，Release 当日现势复核）
- **Human Review = completed**（test-8.0 人工双队列清零为 gate PASS 前置；主观视觉项→NEEDS_HUMAN_REVIEW 通道保持；Release Readiness 由用户确认）
- **LIVE_SMOKE_TEST = NOT_RUN**
- **REASON = USER_CREDENTIAL_NOT_PROVIDED**（未提供 Key，零真实 API 调用，未伪装 PASS；外部 Provider 代码路径经可注入 transport 全覆盖测试 IMG-01~14=26/26；用户配置 Key 后 `aeromech image test` 一次冒烟即可补验，非阻塞）

## 测试计数（Release 回归，版本升级后全量重跑）

| 套件 | 结果 |
|---|---|
| tests/v1_4 | 11/11 |
| tests/v1_4_1（含 dev_install_sync 13/13 IDENTICAL） | 5/5 |
| tests/v1_5 | 13 文件 / 418 断言 |
| tests/v1_6（含 test_document_contract 66/66，版本态已翻转 v1.6.5） | 14 文件 / 399 断言 |
| tests/v1_6_5（视觉系统 35 + 黄金样本 6 + CFG 18 + RES 12 + FB 14 + SEC 13 + IMG 26） | 7 文件 / 124 断言 |
| test_a_template_fidelity | 12/12 |
| test_b_format_reconstruction | 6/6 |
| secret_leak_qa（repo/diff/artifacts 全 scope） | **FAIL=0**（6 WARN 均为测试自身合成样本） |
| test-8.0 Delivery Gate | **PASS** |
| document contract | 66/66 PASS |
| dev↔install sync | IDENTICAL（版本文件已同步） |

## 视觉 QA

- Academic Visual System：figure_style.py 单一来源（3 轮量化验证调色板/字号阶梯/间距）、figkit role= 语义角色色、mermaid 回退 fail-closed、自证式 PASS 移除（FIG-05/06、GQ-15→SKIP 人工复核）。
- Visual QA：VIS-01~12 入构建链 + Delivery Gate figure_visual 域（test-8.0：19 项通过）；黄金样本 4 类型自检 + 幂等重生成 6 断言；graph_quality 渲染测量校准（刻度标签带裁剪 + 数字墨高 0.72em——修复假 FAIL，9.5pt 标准未放松）；test-8.0 GQ ALL PASS（含图3-2 E6 真实溢出修复，逻辑/数据零改动）。

## Image Provider

- 用户自配 API：image_config.py 解析链 process→skill→`~/.aeromech/.env`（project .env 默认关闭）；SecretStr 全掩码+指纹；image_cli config/status/test/remove。
- OpenAI 兼容后端：image_backends.py b64 生图、原子落盘、8 类错误分类（401=INVALID_CREDENTIAL 不伪装）；可注入 transport。
- 受控生图：ai_figure_gate.py Figure Plan 九字段闸（缺 Plan=FIGURE_PLAN_REQUIRED+零 HTTP）；确定性类型 local 优先；结构化溯源提示词+prompt_hash；provenance 白名单；IMAGE_MAX_ATTEMPTS=3 成本闸+首次费用明示。

## Fallback

- 无 Key=正常态自动回落既有 Figure Pipeline（UNAVAILABLE≠错误）；旧 spec 零 env 接触、与 v1.6.0 字节一致（FB 场景 A）；五类确定性图恒 local 优先；仅 provider_required=external 且无凭据→NEEDS_HUMAN_REVIEW。

## Security

- secret_leak_qa repo/diff/artifacts 三 scope，发布态 FAIL=0（tests/ 白名单 + 行级可审计豁免 secret-scan-exempt）。
- record()/log_action() 边界 redact：lifecycle/JSONL 落盘无凭据；AI provenance 11 字段白名单不含 Key。
- AI 生成视觉≠研究证据：ai_generated_visual 禁 verified/partial（RI-E-DISGUISE critical）；证据种子仅 E/DS/CALC/M。

## 已知限制

- **LIVE_SMOKE_TEST = NOT_RUN（USER_CREDENTIAL_NOT_PROVIDED）**——唯一已知限制，非阻塞；补验路径：`aeromech image config` → `aeromech image status` → `aeromech image test`（一次最小真实生成，调用前明示费用）。
- 其余无。版本升级仅同步版本声明（SKILL/README/CHANGELOG/orchestration.md 状态行 + document contract 版本态翻转）；state.md 无当前版本声明（§18 等为历史归属标注，保持不变）；测试期望值除版本态翻转外零改动；test-8.0 研究数据/结论零改动。

## Git

- Release commit：`release: v1.6.5`（本地唯一；含版本文件 + contract 翻转 + 本报告）
- 不 push / 不 merge / 不 PR / 不 GitHub Release / 不 tag（由用户手动完成）
