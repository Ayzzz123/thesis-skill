# v1.6.5 Release Readiness Check

- 日期：2026-09-19
- 结论：**RELEASE_READY = YES**
- 范围纪律：仅只读检查 + 本报告；零代码/测试/架构修改。

## 1. 工作树

- `git status --short` = 空 → **clean**（HEAD = bcc20c4）

## 2. 分支

- `feature-1.6.5-figure-optimization`（未 push / 未 merge / 无 PR）
- 提交链：6f11ebe（phase-design）→ 50ad344（design rev2）→ 3284a9b（phase2A）→ bcc20c4（final）

## 3. 版本状态

- SKILL.md / README.md / CHANGELOG.md 三处一致于 **v1.6.0 发布态**（tests/v1_6/test_document_contract.py DOC-01/02 锁定联动一致）。
- v1.6.5 由分支名 + 提交历史（phase 前缀提交）标识；本次指令未含版本号提升，未执行。如需 CHANGELOG v1.6.5 条目与版本号升级，属后续发布流程指令。

## 4. 通过证据（现有证据 + HEAD 现势复核）

| 项 | 证据 | 状态 |
|---|---|---|
| v1.6.5 visual system | `figure_style.py` 单一来源 + figkit role= 语义色 + mermaid 回退去伪造 + VIS-01~12（figure_visual_qa）；test_visual_system 35/35、test_golden_samples 6/6；test-8.0 figure_visual 19 项通过、graph_quality ALL PASS | PASS |
| image provider | `image_backends.py`：OpenAI 兼容 /images/generations（b64、原子落盘、<1000B 判失败）+ 8 类错误分类（401=INVALID_CREDENTIAL 不伪装）+ 可注入 transport；IMG-01~14 = 26/26 | PASS |
| user-configured API | `image_config.resolve()`：process→skill→user(~/.aeromech/.env) 链，project .env 默认关闭；SecretStr 全掩码 + 指纹；test_image_config 18、test_image_resolution 12（含 A/B HOME 隔离） | PASS |
| no-key fallback | 无 Key ≠ 不可用：UNAVAILABLE→自动回落既有管线；旧 spec 零 env 接触、与 v1.6.0 字节一致；五类确定性图强制 local；CLI status/test rc=0；test_image_fallback 14 | PASS |
| secret security | record()/log_action() 边界 redact；secret_leak_qa repo/diff/artifacts 全 scope **FAIL=0**（HEAD 复核 6 WARN 均为测试自身合成样本：1 条 sk-TEST- 白名单 + 2 个已标记样本）；test_image_security 13 | PASS |
| Figure Plan gate | 九字段闸：缺失→FIGURE_PLAN_REQUIRED + **零 HTTP**（IMG-07）；结构化提示词全部源自 Plan + prompt_hash=sha256[:16]（IMG-09）；来源白名单 11 字段、类 Key 字段名拒绝 | PASS |
| AI/research boundary | `generation_method=ai_image_model` 标 verified/partial → RI-E-DISGUISE critical（IMG-12）；AI 图不入 Research Evidence，证据种子仅 E/DS/CALC/M | PASS |
| full regression | v1_4 11/11 · v1_4_1 5/5（sync 13/13 IDENTICAL）· v1_5 13 文件 418 · v1_6 14 文件 399 · v1_6_5 7 文件 **124**（HEAD 复核 7/7=124）· test_a 12 · test_b 6 | PASS |
| test-8.0 | thesis_build pipeline 全阶段 PASS；**Delivery Gate: PASS**（HEAD 现势复核）；orchestrator `gate=PASS · blocked=False`；报告 `.aeromech/artifacts/qa/graph-quality-report.md`（ALL PASS，含 GQ-01b 图3-2 真实溢出修复与 GQ-16/17 测量仪器修复） | PASS |

## 5. 真实 API 冒烟记录

- **LIVE_SMOKE_TEST = NOT_RUN**
- **REASON = USER_CREDENTIAL_NOT_PROVIDED**
- 凭据解析链检查（冒烟阶段）：所有 env 来源均无 IMAGE_BACKEND / API Key（`image status`/`image test` rc=0，零 HTTP）。
- 未因 NOT_RUN 修改任何代码或测试；未伪造 PASS；MAX=1 真实调用配额未消耗。
- 用户配置 Key 后（`aeromech image config` → `image test`，调用前明示费用）可随时补做，不阻塞本发布态。

## 6. Git

- release commit：`release: v1.6.5`（仅含本报告文件）
- 不 push / 不 merge / 不 PR / 不 GitHub Release
