# v1.6.0 Phase 5 报告 — 文档与规则层正式收口

> 日期：2026-09-15 · 分支：`feature-1.6.0-full-stack-orchestration`（未触碰 master、未 merge、未 push）
> 范围：指令 §一~十四 文档收口；未进入 test-8.0（Phase 6）；**无新功能开发、无脚本行为改动**（本 Phase 唯一代码类交付是文档一致性测试）。

## 1. 修改文档（commit `99e3776`）

| 文件 | 改动 | 对应指令 |
|---|---|---|
| `references/orchestration.md`（新建，约 190 行） | v1.6 编排层规则总纲：分层模块地图、主循环（LOAD→CONTEXT→ROUTE→PREREQ→EXECUTE→CHECKPOINT→VALIDATE→NEXT；失败 DETECT→DIAGNOSE→RECOVERY→VALIDATE；NHR→双队列；CRITICAL→BLOCK）、StateIO、Checkpoint 模型、Resume+Drift 五类码表、Action 模型 12 动作表、调度矩阵+AI 挂载点表、五策略恢复表、Loop 集成、Figure 契约+生命周期 7 态、Build Contract 七域+流水线+manifest+内容身份、Gate 聚合域+优先级+终局规则、状态词表（item 七态/gate 五态/生命周期态不混用）、通用性纪律、CLI 速查 | §二/§三/§六/§七/§八 |
| `SKILL.md` | ① 头部版本区：正式版本仍 v1.5.0 + 明确标注 v1.6.0 开发中（分支名、Phase 6/7 未完成、"不视为 v1.5.0 交付承诺"）；② §2 MVP 表加 Thesis Orchestrator 行；③ §3 加载表加编排层行（9 脚本+规则文件）；④ §4 路由表：恢复协议升程序化 + 新增"按流程推进"编排入口；⑤ §7 恢复协议加第 7 条（thesis_state resume / orchestrator resume + drift 禁止从 S1 重启）；⑥ §16 Delivery Gate 加 v1.6 条款（统一构建+聚合器五态+缺证据 BLOCK）；⑦ 新增 §21 Full-Stack Orchestration Layer（定位/主循环/入口/四条不可协商规则/旧项目兼容/Phase 状态如实声明） | §一 |
| `references/state.md` | ① §1 字段表加 `checkpoint`（v1.6 additive）；② §15 骨架加 `checkpoint: {}`；③ §10 支持版本注记（读取侧兼容 1.1=附加键；正式 schema_version 升级待 RC 判定）；④ 新增 §18 程序化状态层：StateIO 行为（损坏→留档+回退可解析备份/高版本只读/未知字段保留）、迁移规则执行（拒绝不落盘/revert 触发源/override 确认+milestone 不重复门禁）、Checkpoint 字段与写入时点、Resume（from_start=false、不重做已完成）、Drift 五码表（含处置=人工/ERROR 不静默）、Recovery 入口引用 orchestration.md §8；**明确"本文件仍是语义真源、脚本是执行体、不一致即缺陷（契约测试拦截）"** | §三 |
| `references/delivery-pipeline.md` | ① §1 流程图更新：S8 走 figure_iface、构建走 thesis_build pipeline（旧项目 builder 路径保留标注）、末尾 Delivery Gate=delivery_gate.py 全聚合、产物加 manifest；② 新增 §8.4 Unified Thesis Build 与 Artifact Manifest（契约七域、缺失语义硬纪律、固定顺序+失败四元组+不得假装、双模式纪律、manifest 核验、EMBEDDED 诚实接线）；③ 新增 §8.5 Delivery Gate Aggregator（聚合域、evidence 五件套、五态优先级、**任何 Critical/ERROR 不得伪装 PASS**、总分不参与放行、缺证据=BLOCK、无模板=N/A、旧项目域 N/A） | §四/§八 |
| `README.md` | §11 当前版本保持 **v1.5.0**（正式），其下新增"v1.6.0 development / roadmap"引言块：目标/模块清单/规则文件位置/进度声明（Phase 0~5 完成、Phase 6/7 未做、升级条件=全 PASS）+ 明确"开发分支行为不构成本 README 所述 v1.5.0 能力承诺" | §九 |
| `CHANGELOG.md` | 顶部新增 `[Unreleased]` 条目（**不是** `## v1.6.0` 正式发布格式）：Phase 0~4 逐项能力摘要、Phase 5 文档收口、即将进行 Phase 6/7、状态行"正式版本号仍为 v1.5.0——本节不构成交付承诺" | §十 |

## 2. 新增测试：`tests/v1_6/test_document_contract.py`（66 断言）

十组锁（DOC-01~10），原则"文档写 A、代码 B → FAIL"：

- DOC-01 SKILL 版本=1.5.0、开发中标注在位、README 正式标题行无 v1.6.0（硬规则）；
- DOC-02 CHANGELOG 顶部必须 `[Unreleased]`、`^## v1.5.0` 仍是最新正式条目、三处版本联动一致（与 test_dev_install_sync 同口径）；
- DOC-03 orchestration.md 覆盖 10 主题 + 主循环链 + 失败链 + "禁止从 S1 重启"条款；
- DOC-04 state.md ↔ 代码：§18 在位、checkpoint 路径、**SUPPORTED_SCHEMA={1.0,1.1} 与脚本常量一致**、drift 四码与 `inspect.getsource(drift_checks)` 逐一对应；
- DOC-05 delivery-pipeline ↔ `TB.ALL_STEPS`：流水线顺序逐字节一致、三新模块引用、"不得伪装 PASS"条款；
- DOC-06 词表统一：item 七态/gate 五态文档=代码常量；旧冲突术语（"待人工复核"变体）全文档零出现；
- DOC-07 figure：`plan_figures/generate_figure/validate_figure` 文档↔函数真实存在、生命周期 7 态=代码常量、SKILL"不复制 Figure Engine"承诺在位；
- DOC-08 build contract 七域文档化 + 缺失语义措辞 + `validate_contract(无契约)=NOT_APPLICABLE` 行为验证；
- DOC-09 Gate：聚合域清单、evidence/reason/remediation 五件套、RANK 优先级序=文档规则、"总分不参与放行"同时出现在文档与 `delivery_gate` 源码；
- DOC-10 orchestration.md §15 引用的 9 个脚本文件全部真实存在且可 import，AI 挂载点 4 个执行器在 `execute_ai` 源码中真实接线。

**发现并修正的文档措辞缺口（真实收口价值）**：首跑即抓到 5 处文档≠断言/代码（§7 标题缺"Route"一词、"格式链"应为"格式 QA"、required/optional 语义跨行断句、"总分不参与放行"未在源码 docstring 出现）——修正后全绿；其中 DOC-09 改为对 `inspect.getsource(DG)` 取证而非依赖无 docstring 的函数，锁更真实。

## 3. 代码/文档/测试一致性核对结论

- v1.6 十项能力（指令 §一清单：StateIO/Checkpoint/Resume/Research Context/Stage Routing/Orchestrator/Failure Recovery/Figure Interface/Unified Build/Artifact Manifest/Delivery Gate）全部在 SKILL §2/§3/§4/§7/§16/§21 + orchestration.md + state.md §18 + delivery-pipeline §8.4/§8.5 有对应描述，且每条描述可被 test_document_contract 或功能测试反向验证；**无文档超前于代码**（Phase 6/7 一律标注"未完成/未发布"）。
- 状态词表统一：全仓文档现用词 = v1.4.1 七态（item 级）+ 五态（gate 级终局，SKILL §16/§21、delivery-pipeline §8.1/§8.5、orchestration.md §13 同口径）；BLOCK 仅 gate 级；SKILL.md 历史段落未改动（其旧词表本就被 v1.4.1 §19.1 统一条款覆盖，不产生冲突术语）。
- 版本号链：SKILL=README=CHANGELOG 正式条目=v1.5.0（test_dev_install_sync 13 断言锁）；v1.6.0 仅存在于 [Unreleased] 与"开发中"标注。

## 4. 回归（指令 §十二）

| 套件 | 结果 |
|---|---|
| tests/v1_4 | **11/11** |
| tests/v1_4_1 | **5/5**（dev_install_sync 13 PASS，版本联动含新增文档仍一致） |
| tests/v1_5 | **13/13 / 418** |
| tests/v1_6 | **14/14 / 383**（317 + 文档契约 66） |
| test_a / test_b | 12 / 6 PASS |
| dev↔install | sync→**IDENTICAL** |

未改任何既有测试期望值；Phase 1~4 的 317 断言原样全绿（文档编辑零功能影响得证）。

## 5. 已知限制（如实记录）

1. 文档一致性锁是**结构+关键词+常量级**（路径/状态集/函数存在/词表对齐），不逐段校验语义散文——语义漂移仍需 Phase 6/7 的端到端行为验证兜底。
2. CHANGELOG [Unreleased] 按"指令 §十"用普通 `##` 标题（非 keep-a-changelog 严格格式），但顶部判定锁定 `head[1].startswith("[Unreleased]")`，发布改写时契约测试会同步要求更新。
3. 旧文档历史段（如 SKILL §8 "未实现模块的降级响应"为 v0.x MVP 遗留叙述）保留原文——它是历史能力说明非当前功能承诺；统一词表检查只约束 v1.6 相关段落。
4. `orchestration.md` 的 CLI 速查与脚本 argparse 同步靠 DOC-10（存在性）+ 各功能测试（行为）双向约束，未做 `--help` 文本 diff 级锁定。

**PHASE 5 = PASS**

下一步（等待确认）：Phase 6 = test-8.0 全栈冷启动（新材料/新题目、四断点 resume 实测、七能力联合触发）；Phase 7 = 全回归 + RC 报告 + v1.5.0→v1.6.0 版本升级判定。
